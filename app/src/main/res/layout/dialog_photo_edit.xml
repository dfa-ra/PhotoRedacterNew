<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <com.example.photoredacternew.editDialog.photoDrawer.CustomPhotoDrawView
            android:id="@+id/full_image_draw"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:focusable="false"
            android:layout_margin="20dp"
            tools:ignore="MissingClass" />


        <com.example.photoredacternew.editDialog.editPanel.EditPanel
            android:id="@+id/edit_panel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/map_fragment_buttons_background"
            android:gravity="center"
            android:padding="10dp"
            android:visibility="visible"/>

    </LinearLayout>

    <!-- кнопка закрыть -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:layout_marginRight="10dp"
        android:layout_alignParentRight="true"
        android:alpha="0.8"
        android:background="@drawable/map_fragment_buttons_background">
        <Button
            android:id="@+id/destroy"
            android:layout_width="50dp"
            android:layout_height="50dp"

            android:background="@drawable/cloase_icon"
            />
    </LinearLayout>

</RelativeLayout>