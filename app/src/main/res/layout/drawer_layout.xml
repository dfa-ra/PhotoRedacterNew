<RelativeLayout android:id="@+id/full_image_draw_relative_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:visibility="visible"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <com.example.photoredacternew.viewDialog.photoEditer.CustomPhotoDraw
            android:id="@+id/full_image_draw"
            android:padding="20dp"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:focusable="false"
            tools:ignore="MissingClass"/>

    </LinearLayout>

    <!-- Ваш VerticalSeekBar -->
    <com.example.photoredacternew.viewDialog.photoEditer.customView.VerticalSeekBar
        android:id="@+id/seekBar"
        android:layout_width="wrap_content"
        android:layout_height="300dp"
        android:progress="50"
        android:progressDrawable="@drawable/custom_seekbar_progress_width"
        android:thumb="@color/transperent"
        android:max="100"

        android:padding="12dp"
        android:layout_alignParentEnd="true"
        android:layout_centerInParent="true"
        android:translationX="16dp"
       />

    <com.example.photoredacternew.viewDialog.photoEditer.EditPanel
        android:id="@+id/edit_panel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/map_fragment_buttons_background"
        android:layout_alignParentBottom="true"
        android:gravity="center"
        android:padding="10dp"/>
</RelativeLayout>